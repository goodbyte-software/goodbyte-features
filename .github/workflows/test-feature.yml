name: Test feature
on:
  push:
    branches: "main"
    paths-ignore:
      - .gitignore
      - LICENSE
      - README.md
      - .github/**
      - "!.github/workflows/test-feature.yml"
  pull_request:
    paths-ignore:
      - .gitignore
      - LICENSE
      - README.md
      - .github/**
      - "!.github/workflows/test-feature.yml"
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  test-autogenerated:
    runs-on: ubuntu-latest
    continue-on-error: false
    strategy:
      matrix:
        features:
          - Codechecker
          - dev-dependencies
          - JLink
          - NCS_toolchain
        baseImage:
          - ubuntu:22.04
          - mcr.microsoft.com/devcontainers/base:ubuntu
    steps:
      - uses: actions/checkout@v4

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Generating tests for '${{ matrix.features }}' against '${{ matrix.baseImage }}'"
        run: devcontainer features test --skip-scenarios -f ${{ matrix.features }} -i ${{ matrix.baseImage }} .

  # test-feature:
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       feature:
  #         - Codechecker
  #         - dev-dependencies
  #         - JLink
  #         - NCS_toolchain
  #       baseImage:
  #         - ubuntu:22.04
  #         - mcr.microsoft.com/devcontainers/base:ubuntu
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: "Install latest devcontainer CLI"
  #       run: npm install -g @devcontainers/cli

  #     - name: "Testing feature '${{ matrix.feature }}' against '${{ matrix.baseImage }}'"
  #       run: devcontainer features test -f ${{ matrix.feature }} -i ${{ matrix.baseImage }} .

  test-scenarios:
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      matrix:
        features:
          - JLink
          - NCS_toolchain
          # - Codechecker
          # - dev-dependencies
    steps:
      - uses: actions/checkout@v4

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Generating tests for '${{ matrix.features }}' scenarios"
        run: devcontainer features test -f ${{ matrix.features }} --skip-autogenerated --skip-duplicated .
